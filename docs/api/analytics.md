# API Analytics

## Estado
No hay backend en este repositorio. Este documento queda como plantilla para documentar endpoints reales.

## Endpoints (pendiente)
- `GET /analytics/resumen`
- `GET /analytics/ventas`
- `GET /analytics/pagos`
